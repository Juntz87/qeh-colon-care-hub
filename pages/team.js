import Layout from '../components/Layout'
import useSWR from 'swr'
import { collection, getDocs } from 'firebase/firestore'
import { db } from '../lib/firebaseClient'
const fetcher = async ()=>{ try{ const snap = await getDocs(collection(db,'team')); return snap.docs.map(d=>({id:d.id,...d.data()})) }catch(e){ return [] } }
export default function Team(){ const {data}=useSWR('team', fetcher, {revalidateOnFocus:false}); const team = data || [{name:'Dr. Alice Lim', role:'Consultant (Visiting)', photo:'/team/consultant.jpg'},{name:'Dr. Bernard Wong', role:'Specialist', photo:'/team/specialist.jpg'}]; return (<Layout><div className='bg-white dark:bg-gray-800 rounded-lg shadow p-6'><h1 className='text-2xl font-bold text-qehNavy dark:text-white'>Meet Our Team</h1><div className='mt-6 grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6'>{team.map((m,i)=>(<div key={i} className='p-4 bg-gray-50 dark:bg-gray-700 rounded-lg flex flex-col items-center text-center'><img src={m.photo} alt={m.name} className='w-28 h-28 rounded-full object-cover border-2 border-white shadow-md' /><div className='mt-3'><div className='font-semibold text-qehNavy dark:text-white'>{m.name}</div><div className='text-sm text-gray-600 dark:text-gray-300'>{m.role}</div></div></div>))}</div></div></Layout>) }
